machine:
  python:
    version: 3.5.2

dependencies:
  override:
    - /bin/true

test:
  override:
    - /bin/true

deployment:
  production:
    branch: master
    commands:
      - aws s3 sync . s3://calligre-us-east-1/cloudformation/ --exclude "*" --include "*.yml" --delete
      - aws s3 sync . s3://calligre-us-east-2/cloudformation/ --exclude "*" --include "*.yml" --delete
      - aws s3 sync . s3://calligre-us-west-1/cloudformation/ --exclude "*" --include "*.yml" --delete
      - aws s3 sync . s3://calligre-us-west-2/cloudformation/ --exclude "*" --include "*.yml" --delete
      - aws s3 sync . s3://calligre-eu-west-1/cloudformation/ --exclude "*" --include "*.yml" --delete
      - aws s3 sync . s3://calligre-eu-west-2/cloudformation/ --exclude "*" --include "*.yml" --delete
      